package algorithm

func LongestPalindrome(s string) string {
	//sLen := len(s)
	maxLongest := ""
	//maxLongestLen := len(maxLongest)
	//if sLen > 0 {
	//	maxLongest = s[:1]
	//}
	//bMap := map[byte]int{}
	//for i := 0; i < sLen; i++ {
	//	b := s[i]
	//	if j, ok := bMap[b]; ok {
	//		k := 0
	//		for ; j < maxLongestLen; j++ {
	//			if maxLongest[j] != s[i-j] {
	//				break
	//			}
	//			k++
	//		}
	//		if j ==  > maxLongestLen {
	//			maxLongest = s[j : i+1]
	//		}
	//	} else {
	//		bMap[b] = i
	//	}
	//}
	return maxLongest
}
